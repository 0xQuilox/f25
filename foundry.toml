[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/"
]
[rpc_endpoints]
boba = "${BOBA_RPC_URL}"

[etherscan]
boba = { key = "${ETHERSCAN_API_KEY}" }